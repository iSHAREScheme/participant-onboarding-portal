# Read by SonarCloud AUTOMATIC ANALYSIS (which ignores sonar-project.properties).
# Locale dictionaries are parallel key->string translations with no logic;
# copy-paste detection flags en.ts vs nl.ts against each other by construction.
sonar.exclusions=frontend/src/locales/**
